deepagents>=0.7.4
langgraph>=1.2
langchain>=1.3
langchain-ollama>=1.1                # the only model plane: Ollama

langgraph-checkpoint-postgres>=3.1  # durable thread state (DATABASE_URL)
psycopg[binary,pool]>=3.2           # ditto

python-dotenv>=1.0                  # scripts/verify_stack.py loads .env directly

fastapi>=0.115               # agent/files: the upload/download HTTP app
                              # (FastAPI, not plain Starlette — aegra's http.app
                              # mount loader requires a FastAPI instance)
starlette>=1.6                # agent/files: Response directly
python-multipart>=0.0.9      # agent/files: multipart/form-data upload parsing

openpyxl>=3.1                 # agent/output_writer.py: write_xlsx, agent/file_reader.py: read_xlsx
python-docx>=1.1              # agent/output_writer.py: write_docx, agent/file_reader.py: read_docx
python-pptx>=1.0              # agent/output_writer.py: write_pptx, agent/file_reader.py: read_pptx
pypdf>=5.0                     # agent/file_reader.py: read_pdf
xlrd>=2.0.1                    # agent/file_reader.py: read_xlsx's legacy .xls fallback
                                # (openpyxl only reads the zip-based xlsx/xlsm formats)

httpx>=0.28                   # agent/web_search.py: Tavily search + fetch_url (Tavily-only,
                               # no SEARCH_PROVIDER dispatch — see docs/adr/0007)

pytest>=9.0
blockbuster>=1.5                    # event-loop-safety guard, tests/test_server_compat.py
xlwt>=1.3.0                         # tests/test_file_reader.py only — generates a real legacy
                                     # .xls fixture (nothing in this project writes .xls)
aegra-cli==0.10.5                   # tests/test_aegra_host_http_mount.py only — must match
                                     # aegra-host/requirements.txt's pin
